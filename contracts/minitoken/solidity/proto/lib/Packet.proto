syntax = "proto3";

option go_package = "pkg/ibc/app";

message MiniTokenPacketData {
    // the requester address
    bytes requester = 1;
    // the disclose address on the destination chain
    bytes disclose = 2;
}
